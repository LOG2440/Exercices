export const ACTIONS = {
    ADD: "addProduct",
    DELETE: "deleteProduct",
    EMPTY: "emptyCart",
};
export default function reducer(state, action) {
    switch (action.type) {
        default:
            return state;
    }
}